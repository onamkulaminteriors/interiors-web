/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath is not needed for Vercel unless you want your site at /subpath
    // output: 'export' is not needed for Vercel
};

export default nextConfig;
